{
  "state": { "message": "Build failed because a required dependency is missing." },
  "questions": {
    "next": {
      "type": "choice",
      "instructions": "Does this message call for investigating a build failure, or ignoring normal progress?",
      "criteria": { "inspect": "Investigate a reported build failure", "ignore": "Normal progress without failure" }
    },
    "failed": { "type": "noul", "instructions": "Does the message explicitly report a failed build?" },
    "severity": {
      "type": "score",
      "instructions": "How much does this message block completion of the build?",
      "criteria": ["No obstruction", "A warning but the build can complete", "The build cannot complete"]
    }
  }
}
